# Historical variable-length firmware snapshot

Snapshot saved during the variable-HOST-SPI transition. It documents a four-module ESK v4 FSR-only bridge with actual frame-length HOST SPI transactions, a 1044-byte FULL maximum, DELTA masks, a Teensy 4.1 MUL1 v2 bridge and 10 MHz HOST SPI.

This directory is provenance only. Use `../../../active/four-module-variable-spi` for current build and deployment commands.

