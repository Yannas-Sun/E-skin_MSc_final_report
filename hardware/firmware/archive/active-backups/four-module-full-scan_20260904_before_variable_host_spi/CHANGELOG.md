# 当前四模块 FSR-only 版本修改日志

## 2026-09-01

- 在 PC parser 中加入累计传输检查：外层/内层 CRC 错误、MUL1 序号间断、推断丢帧、重复帧、乱序帧、协议格式错误和 Delta 基础序号失配。
- 在 GUI 状态栏显示上述检查结果，便于直接验证 USB 数据链路和 Delta 缓存状态。
- 保留原有 FULL/DELTA 解析、自同步和错误提示逻辑。
- 增加启动后的自动 30 s 实验记录：保存原始 MUL1 数据、逐包 CSV、JSON 总结，并在当前 `pc` 文件夹生成本次实验 Markdown 日志。
- 调整实验输出结构：每次运行在 `docs/Final/data scalability/data/` 下创建独立时间戳文件夹，所有数据文件和实验日志统一保存其中。
- 将自动记录改为 GUI `Save 30 s` 按钮触发；从按钮触发后的下一个完整 MUL1 包开始计时，并为每个 MUL1 记录实际开始时间戳。
- 增加独立的记录状态通道；GUI 现在显示等待下一帧、已记录秒数、剩余秒数和最终实际记录时长，避免高速数据帧覆盖提示。
- 在最终结果、`summary.json` 和实验日志中增加记录窗口内的完整 MUL1 数量统计。
- 将实验 `20260901_182704` 标注为 200 Hz、单模块 M0、FULL 测试结果。
- 将该次实验文件夹重命名为 `docs/Final/data scalability/data/M0`，并同步更新文件内路径。
- 将 20260901_183546 实验标注并重命名为 `docs/Final/data scalability/data/M1`，条件为 200 Hz、单模块 M1、FULL。
- 将 20260901_183900 和 20260901_184014 分别更新为 `M2` 和 `M3`，并同步写入测试条件及文件路径。
- 将四组 100 Hz 单模块数据按现有规则归档为 `data/N=1/100Hz/M0–M3`，并同步更新结果文件和实验日志路径。
- 将四组 50 Hz 单模块数据按现有规则归档为 `data/N=1/50Hz/M0–M3`，并同步更新结果文件和实验日志路径。
- 新建 `data/N=2/200Hz/`，按六种双模块组合建立 6 个记录目录，并加入实验记录说明。
- 完成 N=2/200Hz 六组双模块记录，按 `M0_M1` 至 `M2_M3` 归档，补充测试条件、结果汇总和文件路径。
- 完成 N=3/200Hz 四组记录和 N=4/200Hz 两次重复记录，按模块组合归档并补充汇总文档。
