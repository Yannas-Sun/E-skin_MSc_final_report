# Derived analyses

This folder contains generated, provenance-checked views of the raw power records. `current/` is the analysis used by the present report; `retest_reviews/` records selected pointwise retests; and `provenance/` records source-photo relocation and reconciliation. Rebuild these views with the scripts in `../../script/Main/` rather than editing result tables manually.
