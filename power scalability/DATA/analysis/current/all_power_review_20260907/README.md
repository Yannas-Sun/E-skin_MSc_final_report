# Complete Power review

This review records all 15 non-empty M0-M3 combinations under ZERO and MAX labels: 30 configuration entries and 64 selected branch observations. It is the coverage and provenance layer used before the report's pointwise retest merge.

The summary tables are descriptive. Each exact combination/load has one continuous-read record; SD is between-combination spread at fixed N, not repeatability. The 13 blocked-standby rows in ../../../raw/canonical/power_raw.csv are kept as a separate historical comparator and are not counted in this matrix.