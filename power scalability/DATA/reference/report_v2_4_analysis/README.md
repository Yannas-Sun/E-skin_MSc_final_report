# Report v2.4 compiled analysis

This folder is an upstream, provenance-checked analysis layer retained by the current report release. It combines the complete 15-combination ZERO/MAX review with the explicitly selected M0 and M3 pointwise retests.

The tables preserve the distinction between complete configuration entries and partial retest updates. Use source_manifest.json and the provenance_json fields in branch_readings.csv to trace each value. The current report's latest selected-point snapshot is in ../../analysis/current/report_v2_8_analysis/; this folder remains because that snapshot hashes these tables.