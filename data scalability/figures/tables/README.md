# Figure tables

These CSV files are the machine-readable grids behind the theory and scaling figures. The 02 tables separate USB and HOST SPI links; 05 contains measured points and conditional module-count projections; 07 contains the DELTA N-K model. The intersection table records each link against its own bandwidth reference.

The tables are derived outputs. Raw measurements and their provenance remain in data/ and analysis/.
