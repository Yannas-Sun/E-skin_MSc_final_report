# 2026-09-06 录制启动边界审计

本审计只读检查 `data` 中三段已经完成的同批次 FULL、N=1、目标槽位 M0 录制。时间使用 summary 内实验时间（英国当地 +01:00），没有使用文件修改时间。没有改写、合并或补写任何原始文件；下述拼接只在内存中用于验证。

| run_id | 开始时间 | Session gaps / lost 起止 | 完整包数 | 首→末序号 | 段内序号异常 |
|---|---|---|---:|---|---:|
| 20260906_223506_406411 | 22:35:06.406411 | 0→1 / 0→1 | 8003 | 793022→801024 | 0 |
| 20260906_223546_434807 | 22:35:46.434807 | 1→2 / 1→2 | 8009 | 801026→809034 | 0 |
| 20260906_223626_449714 | 22:36:26.449714 | 2→2 / 2→2 | 8007 | 809035→817041 | 0 |

实际时长分别为 40.002801、40.000244、40.001672 秒。前两段首次完整包相对开始时间分别为 14.477 ms、8.072 ms；第三段为 6.203 ms。前两段会话起始 last_mul1_sequence 分别为 793020、801024，因此首包各跨过一个序号；第三段从 809034 正常连续到 809035。并非每一段启动都增加一次错误。

## 直接证据与推断

- R1 和 R2 的 raw 均以 `55 4C 31`（`UL1`）开头，首个完整 `MUL1` 的 raw_offset 均为 1083。R3 从 offset 0 的完整 `MUL1` 开始。
- **R2 是直接可复核的跨文件完整包。** R1 raw 的最后 1 字节 `4D`（`M`）加上 R2 raw 的前 1083 字节，构成长度 1084 B、序号 **801025** 的 MUL1 包；MUL1 外 CRC 和其中 ESKF 内 CRC 均有效。该包的全部字节已经实际收到并保存在两个 raw 文件中，但不在两份 packet_log 的完整包集合内。因此此次 Session lost=1 不是这个包的字节在 USB 链路中消失。
- **R1 包含单字节推断。** 将推定的 `M` 加在 R1 的 1083 B 前缀前，得到序号 **793021**，两层 CRC 均有效。CRC 支持“这是缺少首个 M 的包后缀”，但没有录制前原始文件可直接证明其首字节何时收到，不能把这个推断表述为已记录的跨文件重组。
- 本次录制使用的修复前 monitor 在 `begin_capture()` 调用 `framer.reset()`，后者清空 buffer。该实现能解释为什么启动时已读前缀被丢弃，而后缀仍存入新 raw；R2 给出了与此机制完全一致的实际字节证据。

## 主分析窗口与其他校验

每段前 10 秒仍保存在原文件；以下主窗口为 10 秒之后至本段结束。三段主窗口完整包数为 **6003、6004、6003**，均显示 PASS，review_reasons 为空。

三段主窗口的会话增量：outer_crc_errors、inner_crc_errors、sequence_gap_events、lost_frames、duplicate_frames、out_of_order_frames、format_errors、delta_base_mismatches **全部为 0**。独立 backend 的目标 M0 未更新、内 CRC 错误及 PC parser 错误也全部为 0。三段全部已记录完整包均未发现段内跳号。

全段审计仍保留原始 CHECK REQUIRED：R1/R2 有启动序号事件及未归入完整包的前缀；R3 只有 1 B 尾部边界片段。R1 和 R3 各有 1 B 尾片段，R2 没有。不得为了让结果变为 PASS 修改原始 summary 或计数。

这些结论针对已收到字节和已记录 MUL1 包，不能证明没有任何不可观察的 STM32 物理扫描缺失，也不能由此判断 SPI 时钟是否稳定。修复应保持连续 live parser，独立标识录制前开始、录制内结束的包：后缀属于本段 raw，但跨起点包不计入本段完全落在 raw 内的完整包率、K 或 q。

详细逐段计数、首包时间、raw 前缀和内存 CRC 验证结果见同目录 `audit.json`。
